# Introduction aux base de données NoSQL

## Pourquoi le NoSQL ?

Traditionnellement, on utilise des bases de données relationnelles (comme MySQL, PostgreSQL) qui stockent les données sous forme de **tables** avec des **lignes** et des **colonnes**.

Mais ce modèle montre ses limites dans certains cas :

- 📈 Données massives à traiter (Big Data)
- 🔄 Structure des données très variable
- ⚡ Besoin de très hautes performances (temps réel, faible latence)
- 🌍 Applications distribuées (dans le cloud)

C’est dans ces cas-là que les bases **NoSQL** deviennent intéressantes.

---

## Que veut dire « NoSQL » ?

> ✨ **NoSQL = Not Only SQL**

Ce n’est **pas** une opposition au SQL, mais une **autre manière** de stocker les données, souvent plus souple et adaptée à certains usages modernes.

---

## Les différents types de bases NoSQL

Il existe **plusieurs familles** de bases NoSQL, selon la manière dont elles organisent les données :

| Type de base       | Exemple   | Structure des données   | Cas d’usage typique                 |
| ------------------ | --------- | ----------------------- | ----------------------------------- |
| 🔑 Clé/Valeur       | Redis     | Non structuré           | Cache, sessions, file d’attente     |
| 📄 Orienté document | MongoDB   | Semi-structuré (JSON)   | Applications web, données flexibles |
| 📊 Orienté colonnes | Cassandra | Structuré               | Big Data, analytics                 |
| 🔗 Orienté graphe   | Neo4j     | Relations entre entités | Réseaux sociaux, recommandations    |

---

## Focus : MongoDB (orienté document)

MongoDB est l'une des bases NoSQL les plus utilisées.

### ✅ Caractéristiques

- Stocke les données sous forme de **documents JSON**
- Les documents sont regroupés dans des **collections** (équivalent des tables)
- Très rapide en lecture/écriture
- Schéma souple : chaque document peut avoir un contenu légèrement différent
- Peu adapté aux **modifications fréquentes** de structure

---

### 📘 Exemple de document MongoDB

```json
{
  "_id": "abc123",
  "nom": "Alice",
  "age": 30,
  "hobbies": ["lecture", "musique"]
}
```

## Concepts clés de MongoDB

| Terme        | Description                                     |
| ------------ | ----------------------------------------------- |
| 🗂️ Collection | Groupe de documents (≃ table en SQL)            |
| 📄 Document   | Objet JSON (≃ ligne dans une table)             |
| 🆔 ObjectId   | Identifiant unique pour chaque document (`_id`) |

## Créer des liens entre documents

MongoDB **ne gère pas** automatiquement les relations comme SQL, mais on peut :

- **Imbriquer** des sous-documents

```json
{
  "nom": "Bob",
  "adresse": {
    "rue": "12 rue du Code",
    "ville": "Paris"
  }
}
```

- **Référencer** d'autres documents (via un `_id`)

```json
{
  "nom": "Charlie",
  "ami_id": "abc123"  // référence à un autre document
}
```

> ⚠️ Trop de références nuisent aux performances.

## Syntaxe MongoDB vs SQL

Voici quelques équivalences utiles pour s’y retrouver :

| SQL                                         | MongoDB (JavaScript)                                    |
| ------------------------------------------- | ------------------------------------------------------- |
| `SELECT * FROM users WHERE name LIKE '%m%'` | `db.users.find({ name: /m/ })`                          |
| `SELECT * FROM users WHERE id IN (1,2,3)`   | `db.users.find({ _id: { $in: [1, 2, 3] } })`            |
| `INSERT INTO users (...) VALUES (...)`      | `db.users.insertOne({ ... })`                           |
| `UPDATE users SET age = 30 WHERE id = 1`    | `db.users.updateOne({ _id: 1 }, { $set: { age: 30 } })` |

## Pourquoi choisir MongoDB ?

| Avantage             | Description                                 |
| -------------------- | ------------------------------------------- |
| 📖 Lecture rapide     | Idéal pour les applications web             |
| 🔁 Réplication facile | Haute disponibilité                         |
| ⚖️ Scalabilité        | Ajout de serveurs possible                  |
| 🔄 Schéma souple      | Documents semi-structurés                   |
| ⚙️ Format BSON        | Format binaire optimisé pour la performance |

## ➡️ À retenir

✅ NoSQL ≠ "sans structure", mais plutôt flexibilité

✅ MongoDB est orienté document, pas orienté table

✅ Il faut choisir NoSQL en fonction du besoin, pas par mode

✅ Dénormalisation = meilleure performance de lecture